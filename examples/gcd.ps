program example();
    var x,y:integer;
    function gcd(a,b:integer):integer;
        begin
            if b=0 then gcd:=a
            else gcd:=gcd(b, a mod b)
        end
    begin
        read_int64(x);
        read_int64(y);
        write_int64(gcd(x, y))
    end