#include <xxx>
program example();
    #var 1x,y:integer;
    function gcd(a,b:integer):integer;
        begin
            if b=0 then gcd:=a
            else gcd:=gcd(b, a mod b)
        end  @
    begin$                                                                                                                  $
        read(x);
        read(y);
        write(gcd(x, y));
        write('\xgg');
    end.