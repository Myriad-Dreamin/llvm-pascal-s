#include <xxx>
program example();
    const g = 1;
    var x,y: arr # [ 0.1 # .. 1.1 # ] # of # integers;
    begin
        read(x);
        read(y);
        write(gcd(x, y));
        write('\x11');
    end.